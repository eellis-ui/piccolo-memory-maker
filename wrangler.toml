name = "piccoload"
compatibility_date = "2026-03-30"

# Static-only Worker: no `main` entrypoint, assets are served directly.
# Mirrors the live configuration of version 169 (deployed 2026-05-12).
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
